import {
    keys 
} from 'underscore';

export function create_table(patient){
    console.log(patient);

    var data_keys = keys(patient);
    var table = [];

    for(var i=0;i<data_keys.length;i++)

}